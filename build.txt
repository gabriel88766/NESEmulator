Downloading SDL:

mkdir vendored
cd vendored
git clone https://github.com/libsdl-org/SDL/tree/release-2.32.x
cd SDL
git checkout release-2.32.x

Building the project:

cmake -S . -B build
cmake --build build

Apply changes:

cmake --build build
